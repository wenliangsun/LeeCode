#include <vector>

#include "TreeNode.h"
using namespace std;

/**
 * 题目105：从前序和中序遍历构造二叉树
 */
class LeeCode105 {
   public:
    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {}

   private:
   TreeNode* buildTree(vector<int>&preorder, vector<int>& inorder,int preIndex, int inoIndex){
       
   }
};