#include <iostream>
#include <vector>

using namespace std;

class Solution {
   public:
    void CountingSort(vector<int>& nums, int maxValue) {
        vector<int> temp(maxValue + 1, 0);
        int index = 0;
        for (int i = 0; i < nums.size(); i++) {
            
        }
    }
};