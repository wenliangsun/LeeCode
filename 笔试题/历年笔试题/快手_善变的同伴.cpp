#include <bits/stdc++.h>

using namespace std;

typedef pair<int, int> PII;

int main() {
    int n, m;
    cin >> n >> m;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    int res = 0;
    cout << res << endl;
    return 0;
}